# Core requirements
streamlit>=1.39.0
Pillow>=9.0.0
googletrans==3.1.0a0
langdetect==1.0.9
gTTS>=2.3.1
SpeechRecognition>=3.10.0
python-dateutil>=2.8.2
numpy>=1.22.0
pandas>=1.3.0

# Optional packages that can be commented out if causing issues
# PyAudio can be installed separately if needed for local development
# pyaudio>=0.2.13
